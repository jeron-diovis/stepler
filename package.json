{
  "name": "stepler",
  "version": "1.0.7",
  "readme": "README.md",
  "description": "Define iteration rules for certain data structure and compute next step value from input data.",
  "main": "build/stepler.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/jeron-diovis/stepler"
  },
  "scripts": {
    "test": "mocha --harmony --recursive --compilers js:mocha-babel",
    "start": "npm test -- --watch",
    "clean": "rm -rf build",
    "prebuild": "npm test",
    "build": "npm run clean && babel -d build stepler.js",
    "push-new-build": "[[ -n $(git status -s -- build) ]] && git add build && git ci -m 'upd. build file' && git push",
    "prepublish": "npm run build && npm run push-new-build"
  },
  "keywords": [
    "iterator",
    "iterate",
    "immutable",
    "step"
  ],
  "author": "Jeron Diovis",
  "license": "MIT",
  "devDependencies": {
    "babel": "^5.8.21",
    "babel-runtime": "^5.8.20",
    "chai": "^3.2.0",
    "mocha-babel": "^3.0.0"
  }
}
